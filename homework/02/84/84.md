# 2.84

```text
    return (ux<<1 == 0 && uy <<1 ==0 ) || // x 和 y 都是 0
            (sx == 1 && sy == 0) || // x <= 0 <= y
            // 理解以下两个表达式，需要弄懂 CSAPP3e P81 第三段中提到的内容
            // "浮点数能够使用整数排序函数来进行排序"
            (sx== 0 && sy == 0 && ux <= uy) || // x， y 非负
            (sx== 1 && sy == 1 && ux >= uy) || // x， y 非正
```
